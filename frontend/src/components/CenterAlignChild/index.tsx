import styled from '@emotion/styled';

export const CenterAlignChild = styled.div({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-around',
  width: '100%',
  height: '100%',
});
